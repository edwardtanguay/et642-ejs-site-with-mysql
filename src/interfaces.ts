export interface IEmployee {
    id: number;
    first_name: string;
    last_name: string;
    job_title: string;
}

export interface ICustomer {
    id: number,
    company_name: string,
    number_of_orders: number;
}